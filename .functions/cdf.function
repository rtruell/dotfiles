# cd into whatever is the forefront Finder window.  wish I could get this to work with Path Finder
function cdf { cd "`osascript -e 'tell app "Finder" to POSIX path of (insertion location as alias)'`"; }
